apiVersion: v1
kind: Service
metadata:
  name: todo-app-selector
spec:
  type: NodePort
  selector:
    app: todo-app
  ports:
  - name: http
    nodePort: 30080
    protocol: TCP
    port: 1000
    targetPort: 3000
